<div class="nav nav-list side-menu animate-dropdown outer-bottom-xs">
    <div class="head"><i class="icon fa fa-align-justify fa-fw"></i> Categories</div>
    <nav class="yamm megamenu-horizontal" role="navigation">
        <ul class="nav">
            {% for categorie in categories %}
                <li class="dropdown menu-item">
                    <a href="{{ path('categorieProduits', { 'categorie' : categorie.id }) }}" class="dropdown-toggle" data-toggle="dropdown">{{ categorie.nom }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
</div>